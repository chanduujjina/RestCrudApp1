# --- JPA & Hibernate ---
```
spring.jpa.hibernate.ddl-auto=update   # options: none, validate, update, create, create-drop
spring.jpa.show-sql=true               # show queries in console
spring.jpa.properties.hibernate.format_sql=true   # format SQL nicely
```
